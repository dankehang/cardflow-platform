{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfig": {
      "phases": {
        "setup": {
          "pkgs": ["nodejs", "npm"]
        },
        "install": {
          "cmd": "cd backend && npm install && npm list express-validator"
        },
        "build": {
          "cmd": "cd backend && echo 'Build completed'"
        }
      },
      "start": {
        "cmd": "cd backend && npm start"
      }
    }
  },
  "deploy": {
    "startCommand": "cd backend && npm start"
  }
}